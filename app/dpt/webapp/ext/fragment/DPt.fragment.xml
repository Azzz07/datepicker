<core:FragmentDefinition xmlns:core="sap.ui.core" xmlns="sap.m">
	<!-- <Button core:require="{ handler: 'dpt/ext/fragment/DPt'}" text="dpt" press="handler.onPress" -->
	<DatePicker id="idInput3"  placeholder="Select Date" core:require="{ handler: 'dpt/ext/fragment/DPt'}" change="handler.change" afterValueHelpClose="handler.close" />
	 
	 <!-- icon="sap-icon://calendar" />
		controllerName="sap.m.sample.DatePickerMassEdit.Page"
		xmlns:mvc="sap.ui.core.mvc"
		xmlns="sap.m">
	<VBox class="sapUiSmallMargin">
		<Table
				id="table"
				mode="MultiSelect"
				items="{
					path: '/table'
				}"
				selectionChange="handleTableSelectionChange">
			<headerToolbar>
				<Toolbar>
					<Title text="Products" level="H2"/>
					<ToolbarSpacer/>
					<Button text="Change Dates" id="changeDatesButton" enabled="false" press="onChangeDatesPress"/>
				</Toolbar>
			</headerToolbar>
			<columns>
				<Column>
					<Text text="id"/>
				</Column>
				<Column>
					<Text text="name"/>
				</Column>
				<Column>
					<Text text="dob"/>
				</Column>
			</columns>
			<ColumnListItem vAlign="Middle" type="Navigation">
				<Text text="{id}" wrapping="false"/>
				<Text text="{name}" wrapping="false"/>
				<DatePicker
						id="datePicker"
						value="{dob}" valueFormat="yyyy-MM-dd" displayFormat="long"/>
			</ColumnListItem>
		</Table>
	</VBox> -->
</core:FragmentDefinition>